---
title: Report
description: Manage Report resources in F5 Distributed Cloud.
---

Report provides tools for retrieving resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-statistics-report-get` | Get Report. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `name` | Name The name of the configuration object to be fetched. | `my-resource` |
| `namespace` | Namespace The namespace in which the configuration object is present. | `system` |

### Query Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `response_format` | The format in which the configuration object is to be fetched. This could be for example  - in GetSpec form for the contents of object... | `GET_RSP_FORMAT_DEFAULT` |


## Example Usage

Ask Claude to help you work with Report resources:

### Get Report Details

> "Get details of the report named 'example' in namespace 'production'"

## CURL Examples

```bash
# Get specific resource
curl -X GET "https://${TENANT}.console.ves.volterra.io/api/report/namespaces/$${PARAM}/reports/$${PARAM}" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}"

```
