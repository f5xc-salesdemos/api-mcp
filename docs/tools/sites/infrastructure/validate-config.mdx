---
title: Validate Config
description: Manage Validate Config resources in F5 Distributed Cloud.
---

Validate Config provides tools for creating resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-sites-validate-config-create` | Validate AWS TGW Config. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `name` | Name Name of the object to be configured. | `example-resource` |
| `namespace` | Namespace Namespace for the object to be configured. | `system` |


## Example Usage

Ask Claude to help you work with Validate Config resources:

### Create Validate Config

> "Create a validate-config named 'example' in the 'production' namespace"

## CURL Examples

```bash
# Create resource
curl -X POST "https://${TENANT}.console.ves.volterra.io/api/config/namespaces/$${PARAM}/aws_tgw_site/$${PARAM}/validate_config" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}" \
  -H "Content-Type: application/json" \
  -d @validate_config.json

```
