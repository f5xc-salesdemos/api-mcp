---
title: K8S Audit Log
description: Manage K8S Audit Log resources in F5 Distributed Cloud.
---

K8S Audit Log provides tools for creating resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-sites-k8s-audit-log-create` | K8s Audit Log Query. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `namespace` | Namespace fetch K8s audit logs for a given namespace. | `system` |
| `site` | Site Site where the K8s Cluster is running. | `-` |


## Example Usage

Ask Claude to help you work with K8S Audit Log resources:

### Create K8S Audit Log

> "Create a k8s-audit-log named 'example' in the 'production' namespace"

## CURL Examples

```bash
# Create resource
curl -X POST "https://${TENANT}.console.ves.volterra.io/api/data/namespaces/$${PARAM}/site/${PARAM}/k8s_audit_logs" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}" \
  -H "Content-Type: application/json" \
  -d @k8s_audit_log.json

```
