---
title: Dc Cluster Group
description: Manage Dc Cluster Group resources in F5 Distributed Cloud.
---

Dc Cluster Group provides tools for creating, and listing resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-sites-dc-cluster-group-create` | DC Cluster Topology. |
| `f5xc-api-sites-dc-cluster-group-list` | DC Cluster Groups Summary. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `dc_cluster_group` | DC Cluster group Name of the DC Cluster group. | `-` |


## Example Usage

Ask Claude to help you work with Dc Cluster Group resources:

### Create Dc Cluster Group

> "Create a dc-cluster-group named 'example' in the 'production' namespace"

### List Dc Cluster Groups

> "List all dc-cluster-groups in the 'production' namespace"

## CURL Examples

```bash
# List resources
curl -X GET "https://${TENANT}.console.ves.volterra.io/api/data/namespaces/system/topology/dc_cluster_groups" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}"

# Create resource
curl -X POST "https://${TENANT}.console.ves.volterra.io/api/data/namespaces/system/topology/dc_cluster_group/${PARAM}" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}" \
  -H "Content-Type: application/json" \
  -d @dc_cluster_group.json

```
