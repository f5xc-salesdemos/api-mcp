---
title: Create Ticket
description: Manage Create Ticket resources in F5 Distributed Cloud.
---

Create Ticket provides tools for creating resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-virtual-create-ticket-create` | Create a ticket for a vulnerability. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `name` | Virtual Host Name... | `example-resource` |
| `namespace` | Namespace... | `system` |


## Example Usage

Ask Claude to help you work with Create Ticket resources:

### Create Create Ticket

> "Create a create-ticket named 'example' in the 'production' namespace"

## CURL Examples

```bash
# Create resource
curl -X POST "https://${TENANT}.console.ves.volterra.io/api/ml/data/namespaces/$${PARAM}/virtual_hosts/$${PARAM}/create_ticket" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}" \
  -H "Content-Type: application/json" \
  -d @create_ticket.json

```
