---
title: Unmerge Sources Openapi Schema
description: Manage Unmerge Sources Openapi Schema resources in F5 Distributed Cloud.
---

Unmerge Sources Openapi Schema provides tools for creating resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-virtual-unmerge-sources-openapi-schema-create` | Unmerge Source from API Endpoint. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `name` | Virtual Host Name Virtual Host name for current request. | `example-resource` |
| `namespace` | Namespace Namespace of the virtual host for current request. | `system` |


## Example Usage

Ask Claude to help you work with Unmerge Sources Openapi Schema resources:

### Create Unmerge Sources Openapi Schema

> "Create a unmerge-sources-openapi-schema named 'example' in the 'production' namespace"

## CURL Examples

```bash
# Create resource
curl -X POST "https://${TENANT}.console.ves.volterra.io/api/ml/data/namespaces/$${PARAM}/virtual_hosts/$${PARAM}/api_endpoint/unmerge_sources_openapi_schema" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}" \
  -H "Content-Type: application/json" \
  -d @unmerge_sources_openapi_schema.json

```
