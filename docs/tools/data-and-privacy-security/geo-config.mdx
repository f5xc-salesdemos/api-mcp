---
title: Geo Config
description: Manage Geo Config resources in F5 Distributed Cloud.
---

Geo Config provides tools for retrieving resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-dataandprivacysecurity-geo-config-get` | Get Geo Config. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `name` | Name The name of the configuration object to be fetched. | `my-resource` |
| `namespace` | Namespace The namespace in which the configuration object is present. | `system` |

### Query Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `response_format` | The format in which the configuration object is to be fetched. This could be for example  - in GetSpec form for the contents of object... | `GET_RSP_FORMAT_DEFAULT` |


## Example Usage

Ask Claude to help you work with Geo Config resources:

### Get Geo Config Details

> "Get details of the geo-config named 'example' in namespace 'production'"

## CURL Examples

```bash
# Get specific resource
curl -X GET "https://${TENANT}.console.ves.volterra.io/api/config/namespaces/$${PARAM}/geo_configs/$${PARAM}" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}"

```
