---
title: IP
description: Manage IP resources in F5 Distributed Cloud.
---

IP provides tools for listing resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-ddos-ip-list` | Mitigation IPs. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `mitigation_id` | Mitigation ID ID of the mitigation we want to GET the IPs for. | `-` |
| `namespace` | Namespace This request is supported only in system namespace. | `system` |


## Example Usage

Ask Claude to help you work with IP resources:

### List IPs

> "List all ips in the 'production' namespace"

## CURL Examples

```bash
# List resources
curl -X GET "https://${TENANT}.console.ves.volterra.io/api/infraprotect/namespaces/$${PARAM}/infraprotect/mitigation/${PARAM}/ips" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}"

```
