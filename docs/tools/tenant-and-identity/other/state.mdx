---
title: State
description: Manage State resources in F5 Distributed Cloud.
---

State provides tools for listing resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-tenantandidentity-state-list` | List states. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `country_code` | Country code 2 characters country code as described in https://en.wikipedia.org/wiki/ISO_3166-1, column `alpha-2`... | `-` |
| `prefix` | Prefix first two or more characters of the state you look for (can be upper or lower case) | `-` |


## Example Usage

Ask Claude to help you work with State resources:

### List States

> "List all states in the 'production' namespace"

## CURL Examples

```bash
# List resources
curl -X GET "https://${TENANT}.console.ves.volterra.io/api/web/custom/countries/${PARAM}/states/${PARAM}" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}"

```
