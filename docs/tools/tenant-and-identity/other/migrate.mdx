---
title: Migrate
description: Manage Migrate resources in F5 Distributed Cloud.
---

Migrate provides tools for creating resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-tenantandidentity-migrate-create` | Migrate CTM child tenants. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `name` | Name... | `example-resource` |


## Configuration Choices

This resource includes mutually exclusive configuration options:

### migratectmchildtenantsreqchoice

| Option | Description | Recommended |
|--------|-------------|-------------|
| `ct_list_to_target_ctm` | Ct list to target ctm |  |


## Example Usage

Ask Claude to help you work with Migrate resources:

### Create Migrate

> "Create a migrate named 'example' in the 'production' namespace"

## CURL Examples

```bash
# Create resource
curl -X POST "https://${TENANT}.console.ves.volterra.io/api/web/namespaces/system/partner-management/child_tenant_managers/$${PARAM}/child_tenants/migrate" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}" \
  -H "Content-Type: application/json" \
  -d @migrate.json

```
