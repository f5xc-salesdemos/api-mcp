---
title: Aggregation
description: Manage Aggregation resources in F5 Distributed Cloud.
---

Aggregation provides tools for creating resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-cdn-aggregation-create` | CDN Access Log Aggregation Query. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `namespace` | Namespace GET aggregation data for a given namespace. | `system` |


## Example Usage

Ask Claude to help you work with Aggregation resources:

### Create Aggregation

> "Create a aggregation named 'example' in the 'production' namespace"

## CURL Examples

```bash
# Create resource
curl -X POST "https://${TENANT}.console.ves.volterra.io/api/cdn/namespaces/$${PARAM}/cdn_loadbalancer/access_logs/aggregation" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}" \
  -H "Content-Type: application/json" \
  -d @aggregation.json

```
