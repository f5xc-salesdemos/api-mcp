---
title: Alert
description: Manage Alert resources in F5 Distributed Cloud.
---

Alert provides tools for listing resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-observability-alert-list` | Get Alerts. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `namespace` | Namespace namespace to scope the listing of alerts. | `system` |

### Query Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `filter` | HighDiskUsage\", severity=\"critical\"\}" List of matchers to filter alerts by. | `-` |
| `inactive` | If set to true, active alerts will not be returned in the response. | `-` |
| `inhibited` | Show inhibited alerts - alerts that are suppressed if certain other alerts are firing. | `-` |
| `silenced` | Show silenced alerts - alerts that are muted based on the matchers configured in the alert manager. | `-` |
| `unprocessed` | Show unprocessed alerts. | `-` |


## Example Usage

Ask Claude to help you work with Alert resources:

### List Alerts

> "List all alerts in the 'production' namespace"

## CURL Examples

```bash
# List resources
curl -X GET "https://${TENANT}.console.ves.volterra.io/api/data/namespaces/$${PARAM}/alerts" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}"

```
