---
title: All Ns Alert
description: Manage All Ns Alert resources in F5 Distributed Cloud.
---

All Ns Alert provides tools for listing resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-observability-all-ns-alert-list` | Get Alerts. |

## Parameters

### Query Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `filter` | HighDiskUsage\", severity=\"critical\"\}" List of matchers to filter alerts by. | `-` |
| `inactive` | If set to true, active alerts will not be returned in the response. | `-` |
| `inhibited` | Show inhibited alerts - alerts that are suppressed if certain other alerts are firing. | `-` |
| `namespace` | Namespace to scope the listing of alerts. When namespace = "system", all alerts for the tenant will be returned. | `system` |
| `silenced` | Show silenced alerts - alerts that are muted based on the matchers configured in the alert manager. | `-` |
| `unprocessed` | Show unprocessed alerts. | `-` |


## Example Usage

Ask Claude to help you work with All Ns Alert resources:

### List All Ns Alerts

> "List all all-ns-alerts in the 'production' namespace"

## CURL Examples

```bash
# List resources
curl -X GET "https://${TENANT}.console.ves.volterra.io/api/data/namespaces/system/all_ns_alerts" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}"

```
