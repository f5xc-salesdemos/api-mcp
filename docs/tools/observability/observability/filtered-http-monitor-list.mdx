---
title: Filtered HTTP Monitor List
description: Manage Filtered HTTP Monitor List resources in F5 Distributed Cloud.
---

Filtered HTTP Monitor List provides tools for listing resources in F5 Distributed Cloud.

## Tools

| Tool | Description |
|------|-------------|
| `f5xc-api-observability-filtered-http-monitor-list-list` | Get Filtered HTTP Monitor List. |

## Parameters

### Path Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `namespace` | Namespace Namespace in which the suggestions are scoped. | `system` |

### Query Parameters

| Parameter | Description | Example |
|-----------|-------------|--------|
| `filter` | Monitor filter. Filter on monitor name or application name. | `-` |
| `limit` | Limit. | `-` |
| `page` | Page. | `-` |
| `sort` | Sort. Return data should by ORDER BY the sort field. | `-` |


## Example Usage

Ask Claude to help you work with Filtered HTTP Monitor List resources:

### List Filtered HTTP Monitor Lists

> "List all filtered-http-monitor-lists in the 'production' namespace"

## CURL Examples

```bash
# List resources
curl -X GET "https://${TENANT}.console.ves.volterra.io/api/observability/synthetic_monitor/namespaces/$${PARAM}/filtered-http-monitor-list" \
  -H "Authorization: APIToken ${F5XC_API_TOKEN}"

```
